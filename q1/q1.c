#include <stdio.h>

int main(){
    long long int n, l, r;
    scanf("%lld %lld %lld", &n, &l, &r);

    long long int arr[n];

    for (long long int i = 0; i < n; i++) {
        scanf("%lld", &arr[i]);
    }

}